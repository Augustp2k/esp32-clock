# Photos go here

Upload one or more `.jpg`, `.jpeg`, `.png`, `.webp`, or `.bmp` files to this folder.
The workflow sorts filenames alphabetically and chooses one image per Taipei calendar day.
The selected image is center-cropped to the dimensions in `config.json`.

If this folder contains no supported image, the project publishes a placeholder so the workflow can still be tested.
